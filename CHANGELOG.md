## 📝 更新日志

### V2.4.0-lite (2026-08-15)

**🃏 插件页卡片全面补全配置项 + 修复启用状态误判 + 表情包降权入配置面板**

- **🐛 修复启用状态误判**：原判定逻辑"参数组中任一值非零即显示已启用"，导致转发解析（嵌套深度默认 3）、图片识别（范围默认 mention_only）等未开启的功能显示为"已启用"。现改为**主开关判定**——每张卡片/胶囊指定主开关键（如 enable_forward_message_parsing），徽章状态与真实开关一致
  - 戳一戳：模式为 ignore 时显示"已关闭"；Smart 并发、流水线各环节为常驻启用（无"关闭"状态）
- **🃏 卡片配置项补全**（不再只有单个开关）：
  - 黑名单：新增 黑名单用户ID、黑名单关键词 列表编辑（每行一项）
  - 内容过滤：新增 输出过滤规则、保存过滤规则 列表编辑（每行一条规则）
  - 指令&关键词：新增 触发关键词列表 编辑；表情包降权：新增 最低概率
  - 列表类字段均以"每行一项"的文本框编辑，保存自动转数组
- **😀 表情包降权入配置面板**：enable_emoji_filter / emoji_probability_decay / emoji_decay_min_probability 三个键代码中有读取但原版 _conf_schema.json 从未定义（AstrBot 配置页看不到、也无法配置）。现补入新增分组「😀 表情包降权」（配置项 75 → 78）
- **✅ 验证**：13 个回归测试通过；主开关判定模拟测试 10 场景通过；node --check / py_compile 通过

### V2.3.0-lite (2026-08-15) (2026-08-15)

**✏️ 插件页提示词支持直接编辑并保存**