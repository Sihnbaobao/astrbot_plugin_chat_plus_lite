## 📝 更新日志

### V2.2.0-lite (2026-08-15)

**🧩 插件配置按功能分栏（_conf_schema.json 重组为 13 个分组）**

- **问题**：75 个配置项平铺一列，找配置靠滚动，视觉混乱
- **方案**：利用 AstrBot 原生渲染机制（schema 中 `type: "object"` 的键自动渲染为独立卡片分区）将配置重组为 13 个分组，每组一个可折叠卡片区块：
  - ⚙️ 基础配置 / 🎲 概率筛选 / 🧠 读空气AI判断 / 💬 回复生成 / 📦 转发消息解析
  - 🖼️ 图片识别 / 🧠 记忆注入 / 🔑 关键词触发 / 🚫 黑名单与消息过滤 / 👆 戳一戳
  - 🔄 防复读 / ⚡ Smart 并发 / 🧹 内容过滤
- **配置结构变更**：配置存储从平铺改为嵌套（如 `gcp_section_probability.initial_probability`）；**旧版平铺配置首次加载时自动迁移**到分组结构（迁移逻辑 + 兼容读取层 `_cfg`/`_set_cfg`，schema 外临时键不受影响）
- **兼容性**：插件页管理控制台（status/config/save）完全兼容新结构；AstrBot Dashboard 保存整个配置同样按分组结构落盘
- **✅ 验证**：13 个回归测试通过；兼容层模拟测试 5 场景通过（迁移/读取/写入/跳过/全新）；py_compile / node --check 通过

### V2.1.1-lite (2026-08-15)

**🐛 修复插件页卡片/胶囊点击无响应**

- **问题**：管理控制台点击卡片展开配置面板后，再点击其他卡片/胶囊无反应，也不会切换
- **原因**：展开面板通过 replaceWith() 替换页面中固定容器实现；收起时 wrap.remove() 把容器从 DOM 移除，下一次点击时目标容器已不存在 → 抛异常 → 点击失效
- **修复**：改为固定容器方案——面板容器常驻页面，收起时只清空内容不删除容器；点击任意胶囊/卡片可随时切换，点击已展开项可收起
- **增强**：保存配置成功后自动重新展开当前面板并刷新显示（之前保存后面板会收起）
- **✅ 验证**：node --check 通过；连续切换点击逻辑已代码审查确认

### V2.1.0-lite (2026-08-15)

**🗑️ 移除独立 Web 面板 → 接入 AstrBot 插件页（卡片式/胶囊式管理界面）**
